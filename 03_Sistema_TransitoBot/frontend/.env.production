# Production Environment Variables for Docker
# These variables are embedded at BUILD TIME by Vite

# API Configuration - URL del RouterBack en Docker Compose
# Cambiar 'routerback' por el nombre del servicio en docker-compose.yml
VITE_API_BASE_URL=http://localhost:8080

# Disable mock API in production
VITE_USE_MOCK_API=false

# Disable debug mode in production
VITE_DEBUG_MODE=false
