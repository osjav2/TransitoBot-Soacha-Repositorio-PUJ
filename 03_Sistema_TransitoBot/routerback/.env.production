# Production Environment Variables for Docker Compose
# RouterBack - Orquestador de servicios

# FastAPI Configuration
APP_NAME=RASA Chat Orchestrator
HOST=0.0.0.0
PORT=8080
DEBUG=false

# RASA Configuration
# Usar nombre del servicio de docker-compose en lugar de localhost
RASA_URL=http://rasa:5005
RASA_WEBHOOK_PATH=/webhooks/rest/webhook
RASA_TRACKER_PATH=/conversations
RASA_TIMEOUT=30

# BackRag Configuration (Fallback)
# Usar nombre del servicio de docker-compose
BACKRAG_URL=http://backrag:8000
BACKRAG_TIMEOUT=10

# CORS - Permitir acceso desde el frontend
# Ajustar según el dominio real en producción
CORS_ORIGINS=["http://localhost:5173","http://frontend:80","http://localhost"]
